/* 
import { CoffeeQuality, CoffeeProcess, user, auction } from '@prisma/client';

export class CoffeeLot {
  id: string;
  name: string;
  description?: string;
  origin: string;
  harvestDate: Date;
  quality?: CoffeeQuality;
  process?: CoffeeProcess;
  altitude?: number;
  quantity: number;
  cupScore?: number;
  moistureContent?: number;
  status: string;
  sellerId: string;
  images?: string;
  documents?: string;
  createdAt: Date;
  updatedAt: Date;
  
  seller: user;
  auctions: auction[];
} */